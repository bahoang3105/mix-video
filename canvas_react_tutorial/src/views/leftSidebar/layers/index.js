import Header from "./header";
import Layer from "./layer";

const Layers = () => {
  return(
    <div>
      <Header />
      <Layer nameLayer='Layer 1das'/>
    </div>
  );
};

export default Layers;